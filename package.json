{
  "name": "braillebuddy",
  "version": "0.1.0",
  "private": true,
  "description": "BrailleBuddy — a voice-guided braille teaching box (ESP32-S3 + ElevenLabs + Next.js).",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev -w apps/web",
    "build": "npm run build -w apps/web",
    "start": "npm run start -w apps/web",
    "lint": "npm run lint -w apps/web"
  },
  "dependencies": {
    "@electric-sql/pglite": "^0.5.1"
  }
}
