# DermaScout — Python dependencies (Python 3.12)
# Install:  python -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt

# --- camera / vision / 3D ---
depthai==3.6.1            # OAK-D-SR (RGB + stereo depth + on-device SLAM nodes)
opencv-python==4.13.0.92
numpy==2.4.6
open3d==0.19.0           # point cloud -> mesh (Poisson)
trimesh==4.12.2          # mesh -> GLB for the web dashboard

# --- triage ML ---
torch==2.12.0
torchvision==0.27.0
transformers==5.10.1     # HAM10000 ViT classifier
pillow==12.2.0
mediapipe==0.10.35       # patient pose + (with SegFormer) clothing regions

# --- backend / events ---
fastapi==0.136.3
uvicorn==0.49.0
websockets==16.0

# --- voice + control ---
elevenlabs==2.51.0       # patient-facing voice
pyserial==3.5            # ESP32 actuator link
python-dotenv==1.2.2

# --- desktop capture tool (optional) ---
PySide6==6.11.1          # app.py live OAK viewer

# --- dev / testing ---
websocket-client==1.9.0
