# Wokwi for VS Code / Cursor — simulate the firmware without hardware.
# Build first (PlatformIO: `pio run`), then press F1 → "Wokwi: Start Simulator".
[wokwi]
version = 1
firmware = '.pio/build/esp32-s3-devkitc-1/firmware.bin'
elf = '.pio/build/esp32-s3-devkitc-1/firmware.elf'
